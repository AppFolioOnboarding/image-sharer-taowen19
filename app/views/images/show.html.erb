<a href="/">Home Page</a>
<div>
  <% @image.tag_list.each do |tag| %>
      <%= link_to tag, images_url(tag: tag) %>
  <% end %>
</div>

<%= image_tag @image.url, id: @image.id, tags: @image.tag_list %>
